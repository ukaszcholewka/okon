export declare const okon: {
    canvas: {
        setSize: (canvasElement: HTMLCanvasElement, width: number, height: number) => void;
    };
    utils: {
        random: {
            int: (min: number, max: number) => number;
        };
        aloop: (length: number, initElement?: any) => any[];
    };
};
